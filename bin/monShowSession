#!/usr/bin/env python3

import sys, pickle

for f in sys.argv[1:]:
    print("%s:" % f)
    data = pickle.load(open(f, "rb"))
    for k in sorted(data.keys()):
        print("  %-15s %s" % (k, repr(data[k])))
